# PPT Agent 项目协作规则

本文件适用于整个仓库。除非用户在当前任务中给出更明确的要求，所有 Agent 均应遵守以下规则。

## 项目目标

本项目要制作一套模拟 Microsoft Office PowerPoint 使用体验的 PPT 创作产品，但核心创作方式不是向用户提供传统工具库，而是由 Agent 理解需求、规划并制作 HTML 形式的 PPT。

产品应围绕 Agent 驱动的需求理解、内容组织、视觉设计、页面生成、修改、预览与交付建立完整体验，同时保留用户对页面和运行状态的清晰感知。

## 开发期文档原则

- 当前项目处于开发期，允许不同阶段的文档存在不一致。
- 发生冲突时，以最新的用户决定和最新设计文档为准。
- 早期文档仅作为历史参考，用于理解项目背景、需求来源和设计演进，不得覆盖最新决定。
- 判断当前实现状态时，应同时检查最新文档、Git 提交、代码和测试，不能仅依据旧文档推断。

## 数据结构修改原则

- 开发期修改数据结构时，不需要为旧结构保留兼容层、双读、双写或降级分支。
- 不需要为历史开发数据设计迁移方案，可以直接覆盖并统一修改现有结构。
- 前后端、持久化、Schema、测试和相关文档应一次性切换到最新结构，避免新旧协议并存。
- 只有用户明确提出兼容或迁移要求时，才额外设计相关方案。

## Git Commit 规范

Commit 标题优先使用以下格式：

```text
feat: <核心改动概述>

- <用一句话说明一项变动>
- <用一句话说明另一项变动>
```

- 标题应概括本次提交最核心的改动。
- 正文使用无序列表，每个列表项只用一句话描述一项实际变动。
- 避免在同一列表项中混合多个无关改动。

## 临时文件规则

- 生成的 HTML 预览文件必须写入 `docs/discuss/` 目录。
- TODO、分析草稿和其他讨论阶段文件必须写入 `docs/discuss/` 目录。
- 根目录 `tmp/` 仅用于本地临时数据，已被 Git 忽略，不得存放需要协作或提交的内容。
