# 项目协作准则

## 适用范围

本文件适用于整个仓库。除非用户在当前任务中提出更明确的要求，所有 Agent 均必须遵守本文件。

## 产品目标

本项目旨在构建一套模拟 Microsoft Office PowerPoint 使用体验的 PPT 创作产品。产品的核心创作方式不是向用户提供传统工具库，而是由 Agent 理解需求、组织内容、规划设计并生成 HTML 形式的 PPT。

产品能力应覆盖需求理解、内容组织、视觉设计、页面生成、修改、预览与交付，并确保用户始终能够清晰感知页面状态和 Agent 运行状态。

## 文档治理

### 设计依据

- 项目处于开发期，允许不同阶段的文档存在不一致。

- 需求或设计发生冲突时，以最新的用户决定和最新的相关设计文档为准。

- 判断目标设计时，必须优先查阅最新设计文档；判断当前实现状态时，必须结合代码、测试和 Git 提交，不得仅依据文档推断。

- `docs/archieve/` 中的内容均为历史设计文档，仅用于理解项目背景、需求来源和设计演进，不属于当前有效的设计依据。

### 目录与命名

- 后端 Spec Markdown 设计文档必须存放在 `docs/spec/`，并使用 `yyyy-mm-dd-<需求>-design.md` 格式命名。

- 前端 Demo HTML 原型文件必须存放在 `docs/demo/`，并使用 `yyyy-mm-dd-<需求>-demo.html` 格式命名。

- 根目录 `tmp/` 仅用于无需协作或提交的本地临时数据，不得存放需要纳入版本控制的内容。

## 开发期演进

- 修改数据结构时，默认直接切换到新结构，不为旧结构保留兼容层、双读、双写或降级分支。

- 默认不为历史开发数据设计迁移方案，可以直接覆盖并统一现有结构。

- 前端、后端、持久化、Schema、测试和相关文档必须一次性切换到新结构，避免新旧协议并存。

- 仅当用户明确要求兼容或迁移时，才设计相应方案。

## Git 提交规范

提交信息优先使用以下格式：

```text
<类型>: <核心改动概述>

- <用一句话说明一项变动>
- <用一句话说明另一项变动>
```

- 类型应从常见 Git Commit 类型中选取：feat / fix / refactor / perf / chore / docs

- 标题应概括本次提交最核心的改动。

- 正文使用无序列表，每个列表项只用一句话描述一项实际变动。

- 避免在同一列表项中混合多个无关改动。

