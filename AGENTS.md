# 项目协作准则

## 适用范围

本文件适用于整个仓库。除非用户在当前任务中提出更明确的要求，所有 Agent 均必须遵守本文件。

## 产品目标

本项目旨在**构建一套模拟 Microsoft Office PowerPoint 使用体验的 PPT 创作产品**。产品的核心创作方式不是向用户提供传统工具库，而是由 Agent 理解需求、组织内容、规划设计并生成 HTML 形式的 PPT。产品能力应覆盖需求理解、内容组织、视觉设计、页面生成、修改、预览与交付，并确保用户始终能够清晰感知页面状态和 Agent 运行状态。

## 文档规范

### 设计依据

- 项目处于开发期，允许不同阶段的文档存在不一致。
- 需求或设计发生冲突时，**以最新的用户决定和最新的相关设计文档为准**。
- 判断目标设计时，必须优先查阅最新设计文档；判断当前实现状态时，必须结合代码、测试和 Git 提交，不得仅依据文档推断。
- `docs/archieve/` 中的内容均为历史设计文档，仅用于理解项目背景、需求来源和设计演进，不属于当前有效的设计依据。

### UI 设计规范的记录

- 只有用户**明确要求**修改 `DESIGN.md` 或明确要求将某项 UI 设计规范记录到该文件时，才允许更改 `DESIGN.md`。
- 不能擅自将当前正在进行的前端改动自动写入 `DESIGN.md`， 用户提出、确认或验收前端改动，不等于要求记录设计规范。

### 目录与命名

- 后端 Spec Markdown 设计文档必须存放在 `docs/spec/`，并使用 `yyyy-mm-dd-<需求>-design.md` 格式命名。
- 前端 Demo HTML 原型文件必须存放在 `docs/demo/`，并使用 `yyyy-mm-dd-<需求>-demo.html` 格式命名。
- 根目录 `tmp/` 仅用于无需协作或提交的本地临时数据，不得存放需要纳入版本控制的内容。

## 开发规范

### 开发期演进

- **修改数据结构时，默认直接切换到新结构，不为旧结构保留兼容层、双读、双写或降级分支**。
- 默认不为历史开发数据设计迁移方案，可以直接覆盖并统一现有结构。
- 前后端必须一次性切换到新结构，避免新旧协议并存。
- 仅当用户明确要求兼容或迁移时，才设计相应方案。

### 工具使用

- 禁止调用 `Computer Use`、`Browser` 等计算机 / 浏览器自动化工具执行验证操作，所有验证步骤交由用户手动完成。
- 禁止使用 `SubAgent`、`MultiAgent` 等子代理、多代理能力开展开发工作，全部任务须独立完成。

### 前端设计

- [DESIGN.md](DESIGN.md) 记录已确认的全局 UI 设计规范；开展前端界面开发或修改前，必须阅读并遵循其中的约定。
- 必须对齐产品整体 UI 设计，在布局、配色、字体、间距、图标和交互反馈上保持页面之间的**视觉与体验一致性**。
- 以**高效、极简、易用**为原则，优先保证内容清晰和操作直接，避免过度设计、冗余装饰和不必要的交互复杂度。
- 优先复用已有公共组件与设计模式，将可复用的新增模式沉淀为公共组件，避免相同功能出现多套样式或交互。
- **去 AI 化**，避免模板化布局、无意义的卡片堆叠、装饰性渐变和空泛文案；设计应服务于真实内容与使用场景。
- 可参考 `Frontend Design`、`Design Taste Frontend` 和 `Impeccable` 等 skills 提升设计质量，应用时以项目现有设计规范和最新用户决定为准。

### 单元测试

- **不为了覆盖率写测试，也不把覆盖率当作质量目标**。
- **禁止写大量低价值测试，也不要反复运行已经通过或无法提供更多新信息的测试**。
- **小改动或小需求开发，既不必新增测试，也不必每次都运行全量测试。**
- 只有当一段**逻辑复杂、容易出错、或未来会被重构**时才值得投入测试，其余情况保留实现本身即可。
- 测试断言「行为」而非「实现细节」：只校验可观察的输入 → 输出 / 状态变化，避免测试锁定内部实现，导致重构时大面积失效。

### Git 提交

提交信息优先使用以下格式：

```text
<类型>: <核心改动概述>

- <用一句话说明一项变动>
- <用一句话说明另一项变动>
```

- 类型应从常见 Git Commit 类型中选取：**feat / fix / refactor / perf / chore / docs**
- 标题应概括本次提交最核心的改动。
- 正文使用无序列表，每个列表项只用一句话描述一项实际变动。
- 避免在同一列表项中混合多个无关改动。
