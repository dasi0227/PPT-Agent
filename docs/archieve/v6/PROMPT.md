# 提示词与上下文组装优化

| 序号 | 存在问题 | 变更分析 | 优化目标 | 已完成 |
|---|---|---|---|---|
| 1 | 用户表达规范不够明确，各输出出口不一致 | 建立统一中文术语和表达示例，覆盖回复、进度、提问、计划、润色与简报；明确技术原文的保留条件 | 用户看到页码、标题和业务名称，减少内部字段外露 | ✅ |
| 2 | 上下文直接注入原始数据结构 | 为模型组装专用数据视图，去除无用的项目 ID、时间戳等字段；页面保留稳定引用，并附当前页码和标题 | 减少内部信息干扰，保持页面定位准确 | ✅ |
| 3 | 创作判断指导弱于执行协议 | 补充何时询问、何时自行判断，以及叙事组织、内容取舍、图表选择和缺失数据处理策略 | 提升创作质量，减少机械执行与无效追问 | ✅ |
| 4 | 提示词模块与模式需求不匹配 | 讨论和规划模式共享内容质量标准；拆分公共交付要求与执行、渲染验收规则 | 每种模式获得必要指导，减少无关指令 | ✅ |
| 5 | 大量错误恢复细节常驻上下文 | 常驻通用恢复原则，具体错误原因和修复指导随失败结果按需注入 | 缩短常驻提示词，让恢复指导更有针对性 | ✅ |
| 6 | 系统提示与工具 Schema 重复描述合同 | 字段、枚举、约束和示例使用统一定义生成；提示词重点解释资源归属和操作原则 | 避免重复、冲突与协议漂移 | ✅ |
| 7 | 动态项目状态放在历史之前，破坏缓存前缀 | 保留稳定基线和不可变历史；状态变化时追加更新，明确替代关系，无变化则不重复注入 | 扩大连续请求可复用的缓存前缀 | ✅ |
| 8 | 元数据和工具定义包含易变内容 | 调试版本、路径、哈希尽量留在日志；具体页面范围移入动态上下文，工具按模式和实际能力稳定组装，页面权限进入动态状态 | 减少非必要的前缀变化，同时保留后端权限校验 | ✅ |
| 9 | 技能和参考内容重复进入前置上下文 | 初始只提供目录，正文按需加载；已加载内容沿历史保留，压缩后再恢复必要部分 | 减少重复 token 和上下文膨胀 | ✅ |

## 实施与验证状态

1–9 项已按顺序实现并完成静态核对。已对齐最新范围设计：取消对象划分，执行模式默认可修改全局资源及所选页面的设计稿与 HTML；页面越权仍由后端检查。✅ 表示实现完成，不代表实际请求和交互验收通过。

本轮完成代码阅读、调用关系核对、Go 格式化和差异空白检查；未运行编译、单元测试、浏览器自动化或真实模型评测。回归测试及交互验证由用户执行，缓存命中率尚未通过真实请求测量。

最终规则、模式装配表、资源生命周期、新增文件及人工验收步骤见 [提示词与上下文组装重构设计](docs/spec/2026-09-23-prompt-context-refactor-design.md)。
