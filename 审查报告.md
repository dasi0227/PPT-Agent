# 死代码审查报告

- 审查日期：2026-09-24
- 范围：`backend/`（Go）与 `frontend/src/`（TS/TSX）
- 目标：找出已经不再被使用、或与最新设计背道而驰的代码 / 文件 / 函数 / 逻辑

## 方法

| 检查项 | 手段 |
| --- | --- |
| 后端未使用代码 | `staticcheck -checks=U1000`（未导出符号的未使用检测） |
| 后端悬空引用 | `go build ./...` + 全仓 grep（`ThemeContext`、`chrome` 等） |
| 后端死包 | 逐个 `internal/*` 包统计被外部 import 的次数 |
| 前端未使用 | `tsc --noEmit`（`noUnusedLocals` / `noUnusedParameters`） |
| 前端死文件 | 全量扫描每个文件的 import / `React.lazy` / `new URL` 引用 |
| 命名一致性 | 全仓 grep 旧名词（`演示要求`、`全局设计`、`蓝图`、`设计要求`、`chrome`） |

## 结论摘要

- 后端：**24 处死代码**，分布在 15 个文件，均为未导出的函数 / 类型 / 变量，可直接删除。
- 前端：**2 处死代码** —— 1 个整文件死组件、1 个仅被自身测试引用的函数。
- 无死 Go 包；无遗留旧命名；`chrome → decorations` 重命名与 `theme_context.go` 移除均已清理干净（详见第四节）。
- 后端 `go build ./...` 通过，前端 `tsc --noEmit` 通过，当前无编译 / 类型错误。

---

## 一、后端死代码（建议直接删除）

### 1. `internal/workflow/ppt_helpers.go` — 6 个失效帮助函数

上下文重组的遗留（对应提交「重构 Agent 上下文组装」「重构 PPT 创作提示词」），设计读取已迁移至 `contextengine`，这些旧入口不再有调用方：

| 符号 | 行号 | 说明 |
| --- | --- | --- |
| `intValue` | 105 | 类型兜底转 int，已无人调用；删除后 `strconv` import 一并移除 |
| `currentDesign` | 150 | 读取 `design.json` 的旧入口；`currentManifest`/`currentOutline` 仍在用，唯独它被弃用 |
| `readSlideModel` | 181 | 读取单页 `SlideSpec` 的旧入口，已无调用 |
| `schemaEvidence` | 243 | 三类 evidence 工厂仅剩 `newEvidence` 在用，这三个包装器全部失效 |
| `staticEvidence` | 246 | 同上 |
| `referenceEvidence` | 249 | 同上 |

### 2. `internal/workflow/runtime.go` — 3 个（含 1 个连带）

| 符号 | 行号 | 说明 |
| --- | --- | --- |
| `attachmentMessageParts` | 2539 | 被完整版 `referenceMessageParts`（2543）取代的薄封装 |
| `containsMessageText` | 2580 | 无调用（相邻的 `containsRunInstruction` 仍在用） |
| `approximateMessageTokens` | 2908 | 无调用；删除后 `approximateTokens`（2901）随之失效，需一并删除 |

### 3. `internal/service/spec.go` — 3 个

| 符号 | 行号 | 说明 |
| --- | --- | --- |
| `atomicWrite` | 22 | 整段原子写实现已无人调用 |
| `firstNonEmpty` | 57 | 无调用 |
| `nonNil` | 66 | 无调用 |

### 4. 其余单点

| 位置 | 符号 | 行号 | 说明 |
| --- | --- | --- | --- |
| `internal/contextengine/types.go` | `type candidate` | 180 | 段候选应用结构体，已无使用 |
| `internal/model/public_event.go` | `validateQuestionOptions` | 920 | 问题事件校验，已无调用 |
| `internal/model/public_event.go` | `require` | 1259 | 通用必填校验，被更具体的 `requireString` 取代 |
| `internal/pptmutation/mutation.go` | `max` | 524 | 无调用 |
| `internal/service/component.go` | `componentNotFound` | 281 | 无调用；删除后 `errors` import 一并移除 |
| `internal/service/skill.go` | `skillIDPattern` | 20 | 赋值 `repositoryIDPattern` 后从未使用 |
| `internal/service/theme.go` | `themeNotFound` | 187 | 无调用；删除后 `errors` import 一并移除 |
| `internal/workflow/evidence.go` | `sortedEvidenceKinds` | 115 | 无调用；删除后 `sort` import 一并移除 |
| `internal/workflow/render_tool.go` | `limitedBuffer` + `Write` | 391 / 397 | 无调用；删除后 `bytes` import 一并移除 |
| `internal/workflow/session.go` | `(*RunSession).baselineForChange` | 312 | 方法无调用 |
| `schemas/schemas.go` | `cloneMap` | 298 | 无调用 |

> 提示：上述带「import 一并移除」的项，删除死代码时若不同步清理 import，`go vet` / 编译会报未使用 import，可作为删除是否彻底的回归信号。

---

## 二、前端死代码

### 1. `frontend/src/features/workspace/NewProjectHint.tsx` — 整文件死组件

`export const NewProjectHint` 在全仓无任何 import（含测试），是早期工作区改版遗留的死组件，可整文件删除。

### 2. `frontend/src/features/agent/modeMapping.ts` — 仅测试引用的死函数

`createTargetedRun` 只被自身的 `modeMapping.test.ts` 引用，生产代码无任何调用。运行请求的组装已改道（现由 `composerStore` / `runStore` 负责），此函数连同其测试文件可一并删除。

---

## 三、已清理干净（无需处理，仅确认）

以下项在本次审查中确认为**干净**，属「重命名 / 移除已完整执行、无残留」：

1. **`chrome → decorations` 重命名完整**：`backend/internal/runtimeassets/chrome.js` 已删除，`decorations.js` 已通过 `assets.go`（embed）、`router.go`、`export/package.go` 正确接入；后端 Go / schema / 前端 TS 中均无 `chrome`、`ChromeItem`、`chrome_tokens` 残留（仅余 `DeckNavigatorChrome`，那是导航栏「外壳」组件名，与 `design.json` 字段无关）。
2. **`theme_context.go` 移除完整**：全仓无 `ThemeContext` / `theme_context` 残留引用。
3. **旧公开文案零残留**：`演示要求`、`全局设计`、`蓝图`、`设计要求` 在全仓（不含 `docs/archieve/`）均为 0 处。
4. **无死 Go 包**：`backend/internal/*` 下所有包均被外部 import。
5. **前端 store 全部在用**：20 个 store 文件均有生产代码引用。

---

## 四、处理建议

1. **一次性删除第一节、第二节列出的死代码**（合计 26 处），这是纯删除、无行为影响，改动最小风险最低。
2. 删除时同步清理连带失效的 import（`strconv`、`sort`、`bytes`、`errors` 等，见上表标注），并以 `go build ./...` + `go vet ./...` 回归确认。
3. `modeMapping.test.ts` 与 `createTargetedRun` 属「测试即死」，建议一并删除，避免保留只为测试服务的空壳。
4. `docs/archieve/` 下的历史设计文档（含旧名词）按 `AGENTS.md` 属于历史依据，**不纳入本次死代码清理**。
