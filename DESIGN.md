# UI 设计规范

**本文记录已确认的前端 UI 设计约定，作为后续开发和修改界面的依据**。

### 选择按钮的颜色语义

用于批准、修改、拒绝等决策的选择按钮，统一采用绿色、黄色、红色表达操作含义：
- 颜色由选项的语义决定，不按选项排列顺序分配；无需凑齐三种颜色。
- 对先选择、再提交的按钮组，未选中时保持中性色，选中后使用对应的浅色背景和语义色文字、边框。
- 按钮保留明确的文字标签，使用户能够同时通过文字和颜色理解操作。

| 颜色 | 语义 | 示例 | 颜色 Token |
| --- | --- | --- | --- |
| 绿色 | 同意、允许、批准 | 批准执行、允许一次 | `success` / `success-soft` |
| 黄色 | 需要调整、返回修改 | 返回修改 | `warning` / `warning-soft` |
| 红色 | 拒绝、取消、停止 | 拒绝、取消停止 | `danger` / `danger-soft` |

### 主操作按钮使用柔和蓝色

- 普通主操作按钮、提交按钮、确认按钮都统一使用 `bg-accent-soft text-accent`，即浅蓝背景与蓝色文字。
- 浅蓝悬停背景统一使用 `hover:bg-accent-soft`（`#E8EFFF`），不再混用 `accent/15` 等透明度变体，也不恢复为高饱和蓝底白字。

### 焦点状态不添加蓝色描边

- 控件获得焦点时不添加蓝色边框、外圈或轮廓，避免全局与组件样式叠加形成多层线条。
- 保留现有的焦点背景、文字反馈及键盘交互；选中、错误和页面标记等状态的原有样式保持不变。

### 右栏操作按钮

- 计划获批且后端确认进入执行模式后，当前会话输入栏自动切换到开发模式，后续发送沿用开发模式。
- 刷新、断线恢复或重新打开会话时补齐尚未应用的批准切换；同一次批准只同步一次，保留用户随后手动选择的模式，后台会话不改变当前输入栏。
- 润色（polish）入口放在右栏顶部，位于压缩（compact）右侧、提交（commit）左侧；沿用顶部图标按钮与悬浮展开文案样式，输入为空或当前操作不允许时禁用，输入框内不再悬浮放置润色按钮或为其预留空白。
- 输入框采用一体式顶部栏：18px 外圆角、浅灰顶部与白色输入内容区，左侧模式、右侧范围常驻显示图标、名称和展开箭头；悬浮、聚焦及展开时使用 soft blue。模式不压缩，范围限制宽度、单行省略，悬浮展示完整名称。
- 模式与范围复用公共菜单，输入框靠近页面底部，两者统一优先向上展开；通过 Portal 和空间避让自动翻转、约束可用高度，长列表内部滚动，不被输入框或侧栏裁切。
- 输入中的命令、模型／目标快捷选择、页面／组件／提示词候选和汇总检索弹窗统一以整个输入组件为定位基准，显示在顶部栏上方 8px，不遮挡模式与范围。
- 范围浮层保留对象与页面两层选择，自选页／章列表合并在同一面板内；入口显示自选数量，全局资源仅显示「全局资源」，保留全局和空项目的可选项限制。
- 底部操作按钮（附件、技能、模型）采用**透明背景、无可见描边**，视觉上直接贴合输入框背景，不形成独立的控件块或分隔感；附件与技能在左，模型及发送／停止在右。
- 底部辅助按钮默认态仅展示**图标**，收起文案，保持输入栏在紧凑宽度下的整洁与低干扰。
- **悬浮时展开文案**，以图标 → 图标的过渡揭示按钮用途；展开仅作为临时反馈，移出后恢复图标态。
- 常规态不引入背景色、阴影或边框，交互反馈主要通过悬浮展开文案与轻微变色传达，避免在输入区堆叠视觉层级。

### DOM 标记注释

- 主预览草稿选框右上角提供红底白色 × 标签，与左侧蓝色编号标签同宽高、同缩放比例，镜像圆角并紧贴选框顶边，不留悬浮间隙、无描边；悬停或键盘聚焦略加深，提示「移除标记 N」。靠近画布边缘时向内避让，放大平移时仍可点击。移除同步清除选框、草稿引用和注释，不修改页面内容、不重排或回收编号，保留 Composer 移除入口。
- Composer 引用列表中的 DOM 标记与图片附件统一固定为 176 × 52px，使用一致的圆角、细边框和垂直对齐。引用列表隐藏滚动条并保留横向滚动。
- 图片附件卡片悬停或内部聚焦时，整张卡片统一使用 soft blue 背景，与 DOM 标记一致；移除按钮仅改变图标颜色，不叠加独立背景。
- Composer 中 DOM 标记左侧使用 38 × 38px 圆角底框展示 DOM 图标，与图片缩略图对齐；右侧两行依次展示「标记 N」和「已注释 / 未注释」，注释去除首尾空白后为空时显示「未注释」。字号、行距和右上角移除按钮位置与图片附件一致。
- DOM 标记悬停、内部聚焦和编辑态由整个容器统一呈现浅蓝背景，包含右侧移除区域；移除按钮悬停仅改变图标颜色，不叠加独立背景。
- 注释以锚定浮层显示在对应标记上方，不撑高输入区；通过 Portal 避免引用列表滚动容器裁切，靠近视口边缘时避让。
- 浮层采用白底、细灰边框、10px 圆角和轻阴影，标题「标记 N · 注释」作为整体使用相同的字号、字重和颜色，底部显示 500 字计数与柔和蓝色「完成」按钮。
- 「收起注释」使用向下箭头并保留内容，「移除标记」使用 × 且始终位于标记本身，两种操作明确分离。
- 注释输入框固定为 80px 高，左右内边距 10px、上下内边距 8px，不允许拖拽缩放，超出内容在输入框内部纵向滚动。
- 点击外部或 Esc 保留注释并收起；点击「完成」或 Cmd/Ctrl+Enter 后聚焦主输入框，支持再次点击标记编辑。

### Timeline 工具调用行

- 已发送消息中的图片与 DOM 标记引用使用一致的紧凑标签：同样的高度、边框、字体和蓝色图标，悬停使用 soft blue。图片标签不显示缩略图，点击复用公共图片预览弹窗并按需加载原图；引用列表隐藏滚动条并保留横向滚动。
- 已发送消息中的标记展开后直接显示注释，不重复展示「标记 N」标题；保留空注释提示及页面或内容已删除的状态提示。
- 工具调用在时间线中统一以 `icon + text` 的紧凑行呈现。
- 各类工具调用行保持一致的图标与文字对齐方式、行间距和上下内边距。
- 收起态的摘要文字仅显示单行，超出可用宽度时显示省略号；图标与展开控件不被文字挤压。
- 支持折叠和展开，展开后显示完整详情，收起后恢复单行摘要。
- 展开内容放在摘要行下方，摘要行仍保持统一布局；使用一致的展开／收起指示。
- 编辑类工具成功后的摘要统一使用「已编辑」，不区分创建和更新；分组摘要沿用相同动词，读取、执行命令和计划步骤保留各自语义。
- 工具分组与历史运行展开后的条目间距统一为 8px，与外层时间线一致；工具分组标题与首条子项之间同样保留 8px。
- 时间线摘要仅计划标题保留加粗；对象名称、命令名称与标题、运行状态和变更摘要统一使用常规字重。
- 编辑类工具统一使用 Pencil 图标，单条活动和分组摘要保持一致。

### 幻灯片画布缩放与平移

- 主预览中的幻灯片放大后，实际页面超出可视区域时自动显示小手光标；按住鼠标左键拖动平移，拖动中显示抓握光标，画布直接跟随指针。
- 只允许沿溢出的方向平移，拖动范围限制在页面边缘；缩小或调整面板宽度时重新约束位置，页面完全可见时居中并恢复普通光标。
- 切换页面或项目时重置平移位置；设计稿、总览与全屏放映不启用此交互。
- 元素选择与区域框选优先于平移，启用标记模式时保留当前画布位置并恢复页面选择交互，退出标记模式后恢复小手拖动。

### 图标统一使用 Lucide

- 界面功能图标统一使用 `lucide-react`，同类操作保持一致的图标含义。
- 默认沿用全局 `1.75` 的描边宽度，保持线性图标风格一致。
- 纯图标按钮提供明确的 `aria-label` 和提示文字，优先复用公共 `IconButton`。
- 消息回退操作统一使用 Undo2 图标。

### 弹窗沉淀并使用公共组件

- 弹窗必须基于公共组件实现，统一遮罩、容器、标题、操作区和关闭交互。
- 确认、表单、选择场景分别优先复用 `ConfirmModal`、`FormModal`、`PickerModal`。
- 新场景在现有公共 Dialog 基础上组合；出现可复用的弹窗模式时，继续沉淀到公共组件，避免各业务页面重复实现。

### 下拉选择与菜单

- 表单单选统一使用公共 `Select`，不使用浏览器原生可见下拉列表；操作菜单继续复用公共 `DropdownMenu`，两者共用面板与选项样式。
- 表单选项列表默认在触发框下方间隔 6px 展开，空间不足时自动向上避让，不采用覆盖触发框的选中项对齐方式。
- 面板采用白底、细灰边框、适度阴影和小圆角；选项悬停、键盘高亮和选中状态使用 soft blue，单选项用勾选标记表达当前值，不添加蓝色焦点描边。
- 选择列表与触发框等宽，长内容可换行，长列表内部滚动；保留禁用、空值选项、方向键选择、文字定位和 Escape 关闭行为。
- 下拉列表通过 Portal 避免被父容器裁切；嵌入编辑卡片时，点击选项不触发卡片的外部关闭逻辑。
- 弹窗及范围选择等复合面板保留现有结构与交互，不替换成表单下拉选择。

### 设置保存

- 不提供页面级「保存全部」；模型卡片「保存」校验并直接持久化当前模型，成功后收起，失败保留编辑内容。
- 模型分配选中后立即保存，保存期间显示进度并阻止重复提交，失败恢复原选择；删除模型确认后直接持久化。
- 每次操作只提交已保存配置与本次变更，不夹带其它卡片的未完成编辑；模型重命名及其引用在同一次请求中保存。
- 未保存的卡片显示「未保存」，可取消本卡编辑；其它操作保存成功不会丢弃这些编辑，刷新和离开时仍提示未保存内容。

### 全局通知统一使用顶部 Toast

- 全局通知必须使用统一的 Toast，显示在视口顶部居中位置。
- 每条通知显示 3 秒后自动关闭，倒计时动画与显示时长一致，并提供手动关闭按钮。
- 普通、成功、警告、错误通知均通过同一套组件呈现，按语义区分图标与颜色。
- 业务功能通过 `toastStore` 的公共通知方法触发通知，统一复用 `GlobalToasts` 展示。

### 命令组件

- 六类命令在运行、完成、失败和停止时共用同一个白底、细灰边框、10px 圆角容器；不再把摘要贴背景、结果另起卡片，也不为图标与命令名添加彩色背景块。
- 头部首行以 16px 中性图标、常规字重命令名、本地时间和元信息呈现身份，时间统一为 `MM-DD HH:mm`；第二行显示 14px 常规字重标题，标题固定单行、超出显示省略号，悬浮可查看完整标题，元信息仍可换行。头部内边距为 14–16px，窄栏为 12px。
- 命令图标：kickoff 使用 SportShoe，handoff 使用 Handshake，commit 使用 GitCommitHorizontal，compact 使用 Gauge，rename 使用 Signature，polish 使用 Sparkles。
- 状态呈现：进行态在容器内展示三阶段进度，当前阶段为蓝色旋转图标，已完成阶段为绿色勾选，未开始阶段为灰色圆环；尚未收到阶段事件时显示旋转图标与「正在准备」。完成态头部仅显示展开／收起箭头，不额外展示成功勾选图标；失败／停止保留容器与同位置重试入口，不展开错误详情。
- 结果与操作区在同一容器内用细线分隔，不嵌套第二张卡片。正文继续复用原有 `LongContent` 的 320px 截断、渐隐遮罩、「展开全部」和「收起」按钮，操作区不参与截断。
- 命令结果默认折叠，刷新、进入会话、历史恢复和执行完成均不自动展开；由用户点击标题或箭头展开，重试时重置为折叠态。
- 交互入口：调整建议输入、重试、复制及主操作优先同一行呈现，窄栏空间不足时自然换行；输入提示为「调整建议（可选）」，支持 Enter 或重试按钮提交，不额外显示输入旁箭头按钮。停止使用红色文字、悬停浅红，重试悬停黄、复制悬停绿，新建会话／应用到输入框使用柔和蓝色主操作样式。
- 内容来源：kickoff、handoff、polish、compact 完成态标题直接用结果 `title`、正文用 `content`，不从正文首行推断标题；复制、新建会话草稿、应用到输入框仅用正文。
- rename 空标题与会话标签一致显示「新会话」；keep 展示执行时的当前会话名，详情显示「保留当前名称：XX」，实际改名时显示旧名称 → 新名称。
- 所有时间线活动均由后端持久化并支持刷新恢复，包括命令的进行、完成、失败和停止状态；恢复后保留标题、正文和重试输入，重试原地替换同一条活动。
- 进度节点：来自真实后端阶段事件，前端对已到达阶段保留至少 450ms 展示，不用定时器猜测处理进度。

### 详情卡片与长内容

- 展开后的详情卡片统一占满时间线内容区，与摘要行整体左右对齐，不为图标预留外部缩进，窄屏保持全宽。
- 摘要与卡片间距为 8px，卡片内部保留统一留白与内容层级。
- 长内容统一复用 `LongContent`，默认限制展示高度并截断溢出内容，通过「展开全部」和「收起」按钮切换；未超出高度时不显示按钮，操作区不参与截断。
