## 【一、】将启动简报与交接简报直接用于新会话

原因：

- 最近已完成 kickoff、handoff 的生成、反馈修订与版本链，当前卡片主要支持复制和重新生成。
- 从简报到接续创作仍需手工新建会话并粘贴，已有上下文整理能力尚未形成完整的交接操作。

功能：

1. 在简报卡片提供“以此版本新建会话”，将用户选中的简报版本放入新会话的首条输入草稿。
2. 携带项目、目标页面和简报来源，允许用户编辑内容、选择模型及交互模式后开始任务。
3. 接手 Agent 重新读取项目当前状态，将简报作为背景；原会话与新会话展示可互相跳转的来源关系。

## 【二、】统一 HTML 画布与预览、渲染行为

原因：

- 当前提示词要求 1600×900，主题与渲染器使用 1920×1080；两者同为 16:9，但 Agent 接收到的画布要求与实际执行环境不一致。
- Web 浏览器只应负责按容器等比缩放显示，PPT 本身需要固定的设计画布；项目应统一以 1920×1080 CSS px 作为幻灯片设计基准。
- 编辑器通过嵌套 iframe 展示页面，截图由另一套运行环境生成；缩放、页码和公共装饰需要共同的运行契约，才能让用户看到的页面与最终交付一致。

功能：

1. 统一画布尺寸来源，并同步主题、提示词、预览、截图及导出；默认以 1920×1080 CSS px 完整支持现有 16:9。
2. 由 Runtime 负责将 1920×1080 设计画布等比缩放和居中，适配编辑区、缩略图与全屏，Agent 专注编写画布内的内容。
3. 统一页码、章节标识与公共装饰的布局和样式解释，使预览与截图使用相同规则。

## 【三、】支持选定多页的批量修改，重构范围选择

原因：

- 当前执行范围只有单页和整套；多页引用提供阅读上下文，还不能表达独立的多页修改范围。
- “只改第 3、5、8 页”“统一这一章的图表”是常见操作，用户需要一次表达范围并持续看到执行进度。
- 范围选择会影响 Agent 任务上下文、工具写入边界、运行恢复和前端状态展示，应在图片引用、区域选择和提示词优化之前先重构清楚。

功能：

1. 重构范围选择模型，支持当前页、选定多页、章节和整套 PPT 等范围，并在输入框与任务状态中清晰展示。
2. 使用稳定页面 ID 保存目标集合，允许读取其他页面作为参考，将写入限制在选定范围；涉及全局设计时明确展示影响范围。
3. 将目标集合贯通上下文、工具调用、运行恢复和完成状态，前端逐页显示待处理、处理中、完成或失败。
4. 对部分完成的任务提供“继续剩余页面”，基于当前产物接续执行，避免重复生成已完成页面。

## 【四、】上传图片与参考图，让 Agent 基于视觉素材创作

原因：

- 当前输入框已支持页面、组件和提示词引用，但缺少用户上传图片与参考图的入口。
- 实际 PPT 创作经常从截图、品牌图、产品图、手绘草图或视觉参考开始，要求用户先手工转述会丢失布局、风格和细节信息。
- 文本文件、表格、PDF 和 Office 文件解析会引入另一套内容抽取链路，本阶段先只支持图片格式，避免扩大实现范围。

功能：

1. 支持拖拽、粘贴和选择图片附件，先接入 PNG、JPG、WebP 和 GIF 静态首帧等浏览器常见图片格式。
2. 将图片保存到项目，展示上传、校验、失败与可用状态，并允许随消息引用。
3. Agent 可按需读取原图或缩略图；结合已有模型能力声明，将图片交给支持视觉的模型理解内容、风格和布局。
4. 保留文件名、尺寸、格式和引用位置等来源信息，供后续修改与核对；上下文压缩后仍可重新读取原始图片。

## 【五、】在页面上选中元素或框选区域后交给 Agent 修改

原因：

- 当前页面引用可以定位“第几页”，但“把右侧这张图缩小”“修改这段文字”仍主要依赖用户描述位置。
- 已有 HTML patch 能力可以执行局部替换，缺少的是从用户看到的区域到 Agent 修改对象的定位链路。

功能：

1. 在主工作区的单页 HTML 预览中提供元素选择和区域框选，将命中的 DOM 结构化快照作为引用插入输入框，不生成截图。
2. 随引用传递页面 ID、HTML revision/hash、1920×1080 画布坐标、DOM 定位指纹、受限 HTML、盒模型和布局样式，Agent 仍可按需读取完整页面。
3. Selection 作为修改意图的软约束，由后端静默合并页面与对象 Scope；Agent 可以处理未选中的必要关联区域，但不能绕过现有模式与写权限。
4. 草稿保留选择时的快照和编号框；页面更新或内容删除时不自动重定位，消息发送成功后清除草稿标记，历史只保留编号与注释交互。

## 【六、】优化和淬炼创作提示词，提升 Agent 任务完成效率

实现记录（2026-09-12）：[创作 Prompt 组装升级与变更总结](docs/spec/2026-09-12-creation-prompt-upgrade-design.md)。提示词、组装与回归测试已落地，真实模型效果评估尚未执行。

原因：

- 当前提示词已具备创作流程和质量指引；下一步应从任务完成效率出发，澄清运行事实、目标定义和工具边界，让 Agent 更快完成真实创作任务。
- html-ppt-skill 明确要求复制模板并保留结构，这是其模板驱动路线的选择；本项目以 Agent 创作为核心，应允许模型自行设计 HTML，参考资产按需使用。
- 当前模型的官方提示指南强调清晰目标、充分上下文、具体示例和按场景触发工具；据此应重新审视为旧能力假设增加的流程约束，不能预设规则越少或越多就一定质量更高。参考：[Claude 提示指南](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices)。

功能：

1. 系统性重写常驻提示词，准确说明任务目标、资源归属、画布环境和工具契约，使 Agent 明确什么算完成、何时继续、何时交付。
2. 保留真实的数据依赖，允许 Agent 根据任务选择逐页完成、分批推进、局部 patch 或重写页面，减少与依赖无关的固定执行顺序。
3. 明确主题与组件的使用边界：延续用户选择的整体视觉方向，同时允许自定义版式、SVG、图表和交互，不强制注入或模仿 HTML 样张。
4. 基于当前 Prompt 的实际失败模式做淬炼：删掉干扰任务完成的约束，补上能减少返工的上下文、示例、工具触发条件和完成标准。
5. 让 Agent 结合用户反馈、页面截图和浏览器诊断决定修改方式；语义与视觉判断保留在模型层，程序负责资源可用性、状态一致性和执行范围等运行事实。

## 【七、】导出可独立使用的 HTML、图片和 PDF

原因：

- 当前已有编辑器预览、全屏和渲染截图，但缺少用户可操作的整套交付入口。
- 预览 HTML 仍依赖应用提供的主题与基础样式接口，复制页面文件不能直接成为完整交付物。

功能：

1. 按当前页面顺序导出独立 HTML 演示包，包含主题、字体、素材和播放代码，离开编辑器后可以打开并翻页。
2. 复用渲染能力导出单页或选定多页 PNG，并提供整套 PDF 导出，统一画布、页码及公共装饰。
3. 导出绑定确定的项目内容版本，展示进度、失败页面与下载入口，避免生成过程中混入不同版本的页面。
4. 对尚未生成或与设计稿不同步的页面明确提示，允许用户先补齐内容或选择已有页面导出。

## 【八、】rollback 到某次的 checkpoint 上，撤销 Agent 的改动

原因：

- 后端已有单页 HTML 版本列表与回滚接口，前端尚未提供相应操作入口。
- 一次 Agent 修改可能同时改变设计稿、HTML、页面顺序或公共设计，仅恢复 HTML 无法完整恢复用户认可的状态。
- 用户真正需要的是回到某次任务完成后的 checkpoint，而不是只查看零散文件历史；该能力涉及状态快照、依赖恢复和冲突处理，设计复杂度较高。

功能：

1. 为每次 Agent 任务完成时生成 checkpoint，记录页面、结构、设计、素材引用和必要运行元数据。
2. 展示 checkpoint 列表、关联任务、修改时间和恢复前预览，允许用户选择某次状态执行 rollback。
3. rollback 时恢复相关文件与持久化状态，重新计算受影响页面的状态，并保留可再次恢复的历史。
4. 处理后续改动与运行中任务的冲突，避免回滚早期 checkpoint 时覆盖用户之后的关键修改。

## 【九、】放映模式、讲稿、页面生命周期与动画控制

原因：

- 当前已有全屏翻页和概览，但缺少按页讲稿的保存、编辑和交付，正式演示仍缺少内容准备能力。
- 当前翻页主要切换 iframe 容器显隐，缺少页面进入、离开与重播的统一通知，HTML 动画可能在页面尚未展示时就已播放。
- 参考项目 html-ppt-skill 将键盘导航、讲稿与动画生命周期放在公共 Runtime 中，这些能力可以独立于其模板体系使用。

功能：

1. 支持按页保存和编辑讲稿，Agent 可基于页面内容生成或修订，讲稿与观众看到的正文分开。
2. 完善放映模式的翻页、当前页状态和页码同步，使编辑器预览、全屏放映和导出播放使用一致规则。
3. 为页面提供进入、离开、暂停和重播事件，允许 Agent 自由编写 CSS/JS 动画，并在离页时清理持续运行的效果。
4. 为截图、缩略图和 PDF 提供静态展示状态，确保关键内容不会因等待动画触发而缺失。

## 【十、】打通项目素材的创建

原因：

- 当前 Agent 可以写页面 HTML，但项目命令主要用于读取，写入仅支持受限的 sed 替换，缺少创建独立素材文件的正式能力。
- 渲染 worker 已能读取项目内资源，前端预览尚缺少对应的项目素材访问链路；图片、字体和公共 CSS/JS 需要在两端都能使用。
- 这是扩展 Agent 创作边界的基础设施能力，涉及项目文件写入、依赖追踪和跨端资源加载，复杂度较高，应在前面核心交互稳定后再推进。

功能：

1. 提供项目内素材的创建与更新能力，支持 Agent 生成的 SVG、数据文件和公共 CSS/JS，并纳入现有改动提交机制。
2. 为素材提供稳定引用，统一前端 iframe、渲染 worker 和导出的资源解析，明确反馈缺失或加载失败的文件。
3. 记录素材与页面的依赖关系；修改共享素材后更新受影响页面的状态和预览。
4. 让 Agent 按需生成图表、示意图和页面交互，现有少量组件继续用于开发验证，组件使用由任务需要决定。
