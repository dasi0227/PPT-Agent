# Runtime 迭代 TODO：让 LLM 更稳定地生成 HTML PPT

> 背景：核心生成链路（单一 ReAct Runtime + `mutate_ppt` 类型化操作 + Completion Gate + 证据/物化校验 + Chromium 渲染诊断）已经跑通。最近的工作集中在 core 之外（仓库、slash 命令、上下文窗口、简报卡片）。这份 TODO 回到 html-ppt 需求本身，目标只有一个：**同样的输入，让模型每次都能生成质量稳定的 PPT**。
>
> 现状判断：Runtime 目前保证的是「产物存在且结构合法」（有 spec、有 HTML、有渲染证据、无几何溢出），但**不保证「产物好看且一致」**。质量目前靠提示词软约束，缺少机器校验和度量。下面的开发历程围绕「先能度量、再能约束、再能修复」这条主线展开。
>
> 建议推进顺序：模块 1（度量）→ 能力 1/2（硬校验）→ 模块 2/3 + 提示词（供给侧）→ 能力 3/4/5（收口）。

---

## 一、需要新开发的模块

### 1. 离线生成评测台（golden set + 自动打分 + 回归对比）

【需求分析】
现在无法回答「这次提示词/校验的改动，到底让生成变好了还是变差了」。每次调优都靠人工开一个项目肉眼看，样本少、不可复现、无法回归，导致「稳定性」是一句无法证伪的口号。

【功能简述】
沉淀一组固定的输入用例（不同主题、页数、受众、语言），用脚本批量跑完整生成，收集每页的渲染诊断、校验结果和截图，产出可对比的打分报告，并支持两个版本之间的 diff。

【意义收益】
把「稳定生成」从主观感受变成可度量指标，之后所有提示词和校验的改动都能用同一批用例验证有没有正收益，这是迭代 Runtime 稳定性的地基，缺了它后面所有优化都是盲改。

### 2. 版式与组件资产库扩建（layout primitives + 更多经过渲染验证的组件）

【需求分析】
当前 `seed/assets/components/` 只有 5 个组件，模型大多数时候是在从零手写整页 HTML，而「从零手写」正是溢出、错位、留白失控的主要来源。素材越少，模型越自由，产出越不稳定。

【功能简述】
补齐一套经过渲染验证的版式骨架（封面/目录/两栏/图文/大数字/流程/结尾等安全区网格）和更丰富的内容组件，让模型优先「挑选并填充」而不是「自由发挥」。

【意义收益】
把易错的排版工作前置到已验证的模板里，模型只负责填内容，能显著降低几何和一致性问题的发生率，是提升稳定性性价比最高的供给侧投入。

### 3. 样张 / 示例库（exemplar store，供提示词按版式注入）

【需求分析】
现在的提示词只有「规则描述」（如 rubric 讲要层级清晰），没有一张具体的「好页面长什么样」的 HTML 样张。LLM 对文字规则的遵守远不如对具体示例的模仿，纯规则约束下不同轮次产出的风格会明显漂移。

【功能简述】
维护一批标注好版式类型的高质量 HTML 样张（正例，可选反例），并提供按当前场景检索注入的接口，供 playbook 做 few-shot。

【意义收益】
用具体样张锚定输出风格和结构，比堆砌规则文字更能收敛生成结果，直接减少「同样需求、每次风格不一样」的抖动。

---

## 二、需要实现的能力

### 1. HTML 产出规范的机器校验（补齐 LintSlide / DS-HTML-OUTPUT）

【需求分析】
`internal/designsystem` 的包注释已经承诺提供「slide html 产出规范校验」，但实际只实现了针对 theme 的 `LintTokens`，`LintSlide` 缺失。目前对模型产出 HTML 的静态校验只有 3 条（能解析、含 `.slide-stage`、无硬编码页码），模型完全可以写死 `#fff`、`font-size:12px` 绕开设计系统，导致整套 deck 风格不统一。

【功能简述】
实现 `LintSlide`，机器校验 HTML 是否使用主题 token（而非硬编码颜色/字号）、是否遵守安全区和 stage 尺寸、结构是否语义化等，并把它接进 `mutate_ppt` 的写入门禁（与现有 `validateHTML` 同级）。

【意义收益】
把「用设计系统、别硬编码」从提示词里的口头约定变成写入时就拦截的硬门禁，从源头保证跨页视觉一致，这是稳定性从「靠模型自觉」升级到「靠系统兜底」的关键一步。

### 2. 渲染诊断的视觉质量信号增强 + 结构化修复建议

【需求分析】
渲染 worker 目前只报几何类问题（溢出、越界、console 报错、字体状态）。一张「没溢出但字太小、大片空白、文字挤成一团、对比度过低」的页面照样能通过，所谓「稳定」只是「不崩」，不是「好看」。

【功能简述】
在 `render-worker/worker.mjs` 的诊断里增加视觉质量信号（最小可读字号、文本/背景对比度、空白占比、元素重叠、内容密度），并把每类信号映射成给模型的具体修复动作。

【意义收益】
让渲染这一关不只判断「能不能看」，还能判断「好不好看」，配合截图回传形成更有效的自我修复闭环，减少需要人工返工的低质量页面。

### 3. 语义 / 视觉评审真正成为可靠门禁

【需求分析】
`review_completion` 现在是模型「可选自省」的软能力：不调用也能 finish，`Confidence` 硬编码为 0，评审服务为空时直接降级。也就是说，真正判断「内容对不对、好不好」的这一关目前形同虚设，Completion Gate 只查存在性和新鲜度。

【功能简述】
补齐评审的置信度机制、明确在哪些场景（如完整生成、deck 级编辑）评审为强制前置，并处理好服务不可用时的确定性兜底策略。

【意义收益】
在确定性 Gate 之上补上一道语义/视觉质量关，让「跑完了」和「做对了」不再是一回事，显著降低低质量产物被当成成功交付的概率。

### 4. Deck 级视觉一致性校验

【需求分析】
现在渲染和校验都是**单页隔离**的，没有任何跨页检查。多页生成或分批编辑时，很容易出现 A 页和 B 页配色、字体、间距、标题风格漂移，而系统对此完全无感。

【功能简述】
在提交前对整套 deck 做一致性聚合校验（主色/字体/标题层级/边距是否跨页统一），把漂移作为可修复问题反馈给模型。

【意义收益】
补上「单页都合格、整套却不像一套」的盲区，保证交付的是一份风格统一的演示而不是一堆各自为政的页面，这是用户对「专业 PPT」最直观的稳定性感知。

### 5. Design 方向的前置校验

【需求分析】
生成流程是先 `design.write` 定全局视觉、再逐页产出，但此时还没有任何页面，设计方向基本是「盲选」，且目前只对 theme 的 tokens 做齐备校验，没校验这套设计本身是否可用（如对比度是否达标、token 是否自洽）。一旦方向有问题，后面每一页都会继承缺陷。

【功能简述】
在 design 写入后、逐页生成前，增加一道设计自检（token 齐备、关键前景/背景对比度配对合格），必要时补一张 design 预览渲染供确认。

【意义收益】
把可能污染整套 deck 的设计缺陷挡在逐页生成之前，避免「方向错了、返工一整套」，让稳定性从单页维度提升到整体链路维度。

---

## 三、需要完善的提示词

### 1. 给 playbook 注入具体样张（few-shot）

【需求分析】
`empty_deck_generation`、`slide_presentation_edit` 等 playbook 目前只有步骤和规则描述，没有任何「合格页面示例」。模型在缺少参照时更容易发挥出各种风格，是生成抖动的重要来源。

【功能简述】
配合模块 3 的样张库，在相关 playbook 里按当前版式场景注入 1-2 个正例 HTML 片段作为模仿基准。

【意义收益】
用具体范例收敛模型输出，让「稳定」有了可对照的锚点，比单纯增加规则文字更直接有效。

### 2. rubric 从「描述性」升级为「可核对清单 + 触发动作」

【需求分析】
现在的 `ppt_quality_rubric.md` 是一段散文式的质量描述，模型读完往往只是「感觉自己做到了」，缺少逐条自检和对应的修复动作，导致 rubric 的实际约束力偏弱。

【功能简述】
把 rubric 改写成可逐条勾选的检查清单，每条明确「不满足时该调用哪个工具做什么修复」，与 `completion_repair_guide` 对齐。

【意义收益】
让质量标准可执行、可自查、可闭环，减少「读了 rubric 但没真正对照」的情况，提高每一页达标的确定性。

### 3. `completion_repair_guide` 覆盖新增错误码

【需求分析】
上面能力 1/2/4 会引入新的失败类型（HTML 规范不合格、视觉质量不达标、跨页不一致），但现有修复指引只覆盖旧的证据/同步类错误码，新错误出现时模型将不知道如何修复。

【功能简述】
为每一类新增的 lint / 视觉 / 一致性错误码补上明确的修复步骤和对应工具动作。

【意义收益】
保证新加的硬校验不会变成「拦得住但改不动」的死路，让「拦截—反馈—修复」形成完整闭环，这是新校验能真正提升稳定性的前提。

### 4. 版式选择与安全区指引

【需求分析】
即使有了版式套件，模型仍需要知道「什么内容用什么版式、单页密度上限、安全边距」这类经验规则，否则会把过多内容塞进不合适的版式里造成溢出和拥挤。

【功能简述】
在 playbook 或独立模块中补充版式选择决策指引（内容类型 → 推荐版式、每页信息量上限、安全区规范）。

【意义收益】
把资深设计者的排版判断显式化交给模型，从需求侧就避免高风险排版组合，减少后续渲染修复的循环次数。
